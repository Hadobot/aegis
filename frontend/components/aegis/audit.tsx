import { useMemo, useState } from 'react'
import { Clock3, Filter } from 'lucide-react'
import { governanceEvents, agents } from '@/src/fixtures'
import type { GovernanceEvent } from '@/src/types'
import { ActionBadge } from './shared'

const labels: Record<string, string> = { prompt_injection: 'Prompt injection', jailbreak: 'Jailbreak', data_exfiltration: 'Data exfiltration', role_hijacking: 'Role hijacking', encoding_bypass: 'Encoding bypass', multi_turn_attack: 'Multi-turn attack', none: 'No threat' }
export function AuditView({ onSelect }: { onSelect: (event: GovernanceEvent) => void }) { const [filter, setFilter] = useState('all'); const filtered = useMemo(() => filter === 'all' ? governanceEvents : governanceEvents.filter((event) => event.decision.action === filter), [filter]); return <div className="space-y-7"><div className="page-intro"><div><span className="section-kicker">Decision evidence</span><h2>Audit logs</h2><p>Complete decision history across your governed agents.</p></div><div className="filter-control"><Filter size={14} /><select value={filter} onChange={(e) => setFilter(e.target.value)}><option value="all">All actions</option><option value="allow">Allow</option><option value="flag">Flag</option><option value="block">Block</option><option value="escalate">Escalate</option></select></div></div><div className="audit-table"><div className="audit-head"><span>Timestamp</span><span>Event</span><span>Component</span><span>Risk</span><span>Decision</span></div>{filtered.map((event) => <button key={event.id} onClick={() => onSelect(event)} className="audit-row"><span><Clock3 size={13} />{event.timestamp.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}</span><span><b>{labels[event.threat.type]}</b><small>{event.requestId} · {agents.find((a) => a.id === event.agentId)?.name}</small></span><span>{event.component}</span><span>{Math.round(event.threat.combinedRisk * 100)}%</span><span><ActionBadge action={event.decision.action} /></span></button>)}</div></div> }
